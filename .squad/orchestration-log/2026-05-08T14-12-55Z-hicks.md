# Hicks — Container Deployment Topology Analysis

**Date:** 2026-05-08T14:12:55Z  
**Role:** Backend Infrastructure Owner (Hicks)  
**Task:** Investigate setup-script deployment logic for container app variance

## Key Finding

**Current setup-script (`scripts/Setup-Solution.ps1`) deploys a fixed 2-container topology:**
1. `ca-api-{env}` — .NET Tickets API Container App (line 372)
2. `ca-res-{env}` — Python Resolution API Container App (line 496)

**Total:** 2 container apps per environment (dev, test, any).

**No environment-specific branching logic.** Both dev and test execute identical deployment code.

---

## Dev vs Test Discrepancy Resolution

**Dev Extra Containers (ca-mcp, ca-incident, ca-classifier, ca-request, ca-escalation, ca-resolution):**
- **Root Cause:** Manually deployed during hosted-agents migration experiment
- **Status:** Stale or experimental; not part of current single-command test deployment path
- **Recommendation:** Investigate cleanup; dev containers diverge from intended topology

**Test Containers (ca-api-agent-resolution-test, ca-res-agent-resolution-test):**
- **Status:** ✅ CORRECT — Matches scripted deployment
- **Root:** Deployed by `Setup-Solution.ps1`; kept in sync with Bicep IaC

---

## Action Items

1. **Verify Phase 1 Topology Closure:** 2 containers (API + Resolution) complete Phase 1; MCP Server is Phase 2+
2. **Document:** Add clarification to Phase 2 gate criteria that MCP Server deployment is conditional (not in Phase 1)
3. **Test Parity:** If dev shows >2 containers, investigate and re-provision via setup script to restore parity
4. **Stub Cleanup:** Remove or finalize stub modules (`containerapp-mcp.bicep`, `containerapp-agent.bicep`) once Phase 2 direction is decided

---

## Phase 2 MCP Server Consideration

If MCP Server is approved for Phase 2:
- **Effort:** 2–3 days (MCP spec + setup integration)
- **Deployment:** Add to setup script after decision
- **Topology Impact:** +1 container (3 total per environment)

**Status:** Awaiting team decision on Option 1/2/3 from topology decision inbox.
