# Orchestration Log: Bishop — ca-resolution Container App Deployment
**Date:** 2026-05-06T17:25:00Z  
**Agent:** Bishop (AI/Agents Specialist)  
**Session:** Background agent mode  
**Outcome:** ✅ Success

## Task Executed

Deployed Python Resolution API (`ca-resolution-tocqjp4pnegfo`) to Azure Container Apps with managed identity and external ingress, replacing unused `ca-agres` stub.

## Work Summary

### Prerequisites Met
- Dockerfile path fixed: `COPY src/python/resolution_api .` targets build context (`src/python/`)
- Pydantic version constraint relaxed: `>=2.11.0` required by MCP package
- Azure OpenAI resource accessible via managed identity (Cognitive Services OpenAI User role)

### Deployment Steps Completed
1. ✅ Deleted unused `ca-agres-tocqjp4pnegfo` container app
2. ✅ Built image via `az acr build` (cloud build, no local Docker engine)
3. ✅ Created container app `ca-resolution-tocqjp4pnegfo` with:
   - System-assigned managed identity
   - External ingress on port 8000 (public FQDN)
   - Scale: 0–1 replicas (startup time ~15s first request)
4. ✅ Configured environment variables:
   - `AZURE_AI_ENDPOINT` → oai-agentic-res-src-dev
   - `MCP_SERVER_URL` → ca-mcp internal URL
   - `TICKETS_API_URL` → ca-api external URL
5. ✅ Health check passed: `GET /health` returns `{"status":"healthy"}`

### Endpoint Details
- **Container App Name:** `ca-resolution-tocqjp4pnegfo`
- **FQDN:** `https://ca-resolution-tocqjp4pnegfo.graybush-af9ee262.eastus2.azurecontainerapps.io`
- **Port:** 8000 (external)
- **Health Check:** `/health` → `{"status":"healthy"}`

## Impact

- Blazor UI can now call `POST /resolve` on this endpoint to trigger SSE-streamed resolution workflow
- Replaces the previously unused ca-agres stub
- Uses same Azure AI Foundry endpoint as classifier/incident/request agents
- Resolution API and .NET API are now decoupled; Blazor calls Python API directly

## Status
✅ **Deployed and verified**. Endpoint responsive. Ready for Blazor integration.
